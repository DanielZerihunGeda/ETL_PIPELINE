# Redash Dashboard Screenshot Placeholder

After the stack is running and transformed mart models contain data, open http://localhost:5000 and capture the `Traffic Trajectory Overview` dashboard.

Expected panels:

- Run summary
- Vehicles by type
- Coverage by run
